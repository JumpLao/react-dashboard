import React from 'react'
import { useAuth } from '@react-dashboard/auth'
// import { useAuth } from '@react-dashboard/auth'
const Signin = () => {
  const {
    login,
  } = useAuth()

  return (
    <div>
      
    </div>
  )
}

export default Signin
