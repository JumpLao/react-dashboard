{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0",
    "microbundle": "^0.11.0"
  },
  "scripts": {
    "start": "lerna run start --parallel --no-bail",
    "lerna": "lerna",
    "deploy": "lerna run build && lerna publish"
  },
  "workspaces": [
    "packages/*",
    "app",
    "server"
  ]
}
